apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-backups
  namespace: blog-ghost
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
